#!/bin/bash
# Git Hook - Pre-commit
# 此 hook 委派給 scripts/ollama_review.sh 執行代碼審查
# 消除代碼重複，統一邏輯維護於 scripts 資料夾

# Source the shared library
# 使用 readlink -f 解析符號鏈接的真實路徑，而不是 dirname "$0"
HOOK_DIR="$(dirname "$(readlink -f "$0")")"
# shellcheck source=./lib.sh
source "$HOOK_DIR/lib.sh"

# Determine repository root
if ! get_repo_root; then
  exit 1
fi

# Call the review script with --git-hook flag to enable non-interactive mode
"$REPO_ROOT/scripts/ollama_review.sh" --git-hook "$@"
